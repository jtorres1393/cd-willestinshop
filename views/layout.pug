doctype html
html
    head
        meta(name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no")
        link(rel='stylesheet' media="all" href='/static/css/server.css')
        script(src='//cdn.quilljs.com/1.3.6/quill.min.js')
        link(href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet")
        script(src='/static/js/Sortable.min.js')
    header
        title Wille's Tin Shop - #{page}
    Body
        #vidLoader.bgRed.relative(style="width: 0; height:20px;")
            #count.tWhite
        #settings(onclick="toggleMenu(this)")  
        .fullStage.eCenter.ptMed
            .fullWidth.bBotSm.bGray.mbLrg.pbXs
                .loginMenu.col23.flex.colSkip.tCap
                    .col23.flex.flexStart.navLink
                        a(href="/admin/invoices")
                            .tBold invoices
                        a(href="/admin/shop")
                            .tBold shop
                        a(href="/admin/ac")
                            .tBold Sales
                        a(href="/admin/news")
                            .tBold news
                        a(href="/admin/info")
                            .tBold info
                        a(href="/admin/vendors")
                            .tBold vendors
                        a(href="/admin/users")
                            .tBold users
                        a(href="/admin/mail")
                            .tBold mail
                    .col3.logout
                        a(href="/admin/logout")
                            .tBold.tRight Logout
            #mainPage.fullWidth
                block content
                br
                footer.pbLrg

    

    script.
        checkMenu()
        function checkMenu(){
            const settings = document.getElementById('settings')
            const menu = document.getElementById("menuPanel")
            if(!menu){
                settings.style.display = "none";
            }
        }

        function toggleMenu(e){
            const settings = document.getElementById('settings')
            const menu = document.getElementById("menuPanel")
            if(settings && menu){
                if (settings.classList.contains('active')){
                    console.log('remove')
                    settings.classList.remove('active')
                    menu.classList.remove('active')
                }
                else{
                    console.log('add')
                    settings.classList.add('active')
                    menu.classList.add('active')
                }

        
            }

        }